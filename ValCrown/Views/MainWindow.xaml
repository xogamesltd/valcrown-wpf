<Window x:Class="ValCrown.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="ValCrown" Height="720" Width="1100"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip">

  <Window.Resources>
    <Style x:Key="TitleBtn" TargetType="Button">
      <Setter Property="Width" Value="46"/>
      <Setter Property="Height" Value="32"/>
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Foreground" Value="#9090c0"/>
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" CornerRadius="0">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#ffffff18"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>

  <Border CornerRadius="12" Background="#07070f"
          BorderBrush="#1c1c38" BorderThickness="1">
    <Border.Effect>
      <DropShadowEffect Color="#000000" Opacity="0.6" BlurRadius="30" ShadowDepth="0"/>
    </Border.Effect>
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="38"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

      <!-- TITLEBAR -->
      <Border Grid.Row="0" Background="#0e0e1c"
              CornerRadius="12,12,0,0"
              MouseLeftButtonDown="TitleBar_MouseDown">
        <Grid>
          <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0,0,0">
            <Border Width="22" Height="22" CornerRadius="5"
                    Background="#7c6aff" Margin="0,0,8,0">
              <TextBlock Text="V" Foreground="White" FontWeight="Black"
                         FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <TextBlock Text="ValCrown" Foreground="#f0f0ff"
                       FontWeight="Bold" FontSize="13" VerticalAlignment="Center"/>
            <Border Background="#7c6aff" CornerRadius="4" Margin="8,0,0,0" Padding="5,1">
              <TextBlock Text="PRO" Foreground="White" FontSize="9" FontWeight="Bold"/>
            </Border>
          </StackPanel>
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Style="{StaticResource TitleBtn}" Content="&#x2212;" Click="MinBtn_Click"/>
            <Button Style="{StaticResource TitleBtn}" Content="&#x25A1;" Click="MaxBtn_Click" x:Name="MaxBtn"/>
            <Button Style="{StaticResource TitleBtn}" Content="&#x2715;" Click="CloseBtn_Click">
              <Button.Style>
                <Style TargetType="Button" BasedOn="{StaticResource TitleBtn}">
                  <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                      <Setter Property="Background" Value="#e81123"/>
                      <Setter Property="Foreground" Value="White"/>
                    </Trigger>
                  </Style.Triggers>
                </Style>
              </Button.Style>
            </Button>
          </StackPanel>
        </Grid>
      </Border>

      <!-- WEBVIEW2 CONTENT -->
      <wv2:WebView2 Grid.Row="1" x:Name="WebView"
                    DefaultBackgroundColor="#07070F"/>
    </Grid>
  </Border>
</Window>
